# Aptiro backend - Trust + Export slice
# Core (required to run + test)
fastapi>=0.110
uvicorn[standard]>=0.27
sqlmodel>=0.0.14
pydantic>=2.5
python-multipart>=0.0.9          # file uploads (/api/sources/upload)
httpx>=0.27                      # TestClient

# Production document ingestion (graceful: app degrades, never crashes,
# if a parser is absent - the API returns a clear 4xx instead)
pdfplumber>=0.11                 # PDF text + page map
python-docx>=1.1                 # DOCX in + DOCX export

# Export renderers (Markdown/HTML are stdlib; these add DOCX/PDF)
reportlab>=4.1                   # PDF export

# Production database path (SQLite is the zero-config default; Postgres
# is the production target via APTIRO_DATABASE_URL)
psycopg2-binary>=2.9             # PostgreSQL driver
alembic>=1.13                    # migrations
pgvector>=0.2                    # vector column support (future phases)

# Tests
pytest>=8.0

# Optional: real AI provider (only used when APTIRO_AI_PROVIDER=anthropic;
# the deterministic mock is the default and needs nothing here)
# anthropic>=0.39

# Optional: real embedding provider for the SECONDARY semantic signal
# (only when APTIRO_EMBEDDING_PROVIDER=openai; the deterministic mock is
# the default, needs nothing, and the signal never affects the score)
# openai>=1.0
